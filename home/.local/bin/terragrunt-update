#!/bin/bash

set -e

LOCAL=${LOCAL_INSTALL_FOLDER}/bin
REPO="gruntwork-io/terragrunt"
NAME="terragrunt"
VERSION=$(http GET https://api.github.com/repos/${REPO}/releases/latest | jq -r '.name')

echo Downloading ${VERSION}

cd ${LOCAL}
wget -O ${NAME}-${VERSION} https://github.com/${REPO}/releases/download/${VERSION}/terragrunt_linux_amd64
chmod +x ${NAME}-${VERSION}
unlink ${NAME}
ln -s ${NAME}-${VERSION} ${NAME}

echo Done
